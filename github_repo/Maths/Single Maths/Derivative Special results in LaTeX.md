

# ln(x)
## $\Huge{ \large{\frac{d}{dx}}(\small{\ln(kx)}\large{)} = \large{\frac{1}{x}}}$


$$
\large
{
\begin{aligned}
y =& ln(kx) \\ 
\ln(kx) =& \ln(k) + \ln(x)\\
y =& ln(k) + ln(x)\\
\frac{dy}{dx} =& 0 + \frac{1}{x}\\
\frac{dy}{dx} =& \frac{1}{x}
\end{aligned}
}
$$

# ln(x^k)